import { cards } from "../../constants.js"; 
import { overlay, closeModalWindow } from "../utilies.js";

export class ModalWindowsView {
  constructor(){

    
  }
}
// for (const card of cards){
//   createModalWindow(card)
// }


// function createModalWindow({id, name, ...}){
//   const btn = document.createElement('button');
//   btn.setAttribute('data-row-btn', `${id}`)
// }

// cards.forEach(({img, price, cardName, cardRate, id}) => {
// 
    // function createModalWindow({id, cardName, cardRate, img, price})
    // });

// const btnAddInCart = document.createElement('button');
      // btnAddInCart.classList.add('s-cards__item-wrapper__item__btn', 'btn');
      // btnAddInCart.setAttribute('data-row-btn', `${id}`);


      
// const clickOnCard = () => {

//   createModalWindow();
  
// }

// const addInCard = () => {

//   const todoEntity = {  
//     cardName : `${cardName}`,
//     img : `${img}`,
//     price : `${cardRate}`,
//     cardRate : `${cardRate}`,
//     id : `${id}`

//   };

//   appendTask(cardEntity);
// }
